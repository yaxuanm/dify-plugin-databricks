[project]
name = "databricks_tools"
version = "0.1.0"
description = "Dify tool plugin for Databricks SQL and Jobs APIs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dify_plugin>=0.5.0",
    "requests>=2.31.0",
]

[dependency-groups]
dev = []
